public class DataType {
    public static void main(String[] args) {
        // data type
        // object = pet cat, pet type, age, weight, name
        // datatype variable_name = value;

        String petType = "Cat"; // String = text value
        String name = "Brownie";
        int petAge = 3; // int = integer = whole number
        double petWeight = 1.5;  // double = fraction

        // call your variables back and print
        System.out.println("The pet type is " + petType);
        System.out.println("The pet name is " + name);
        System.out.println("The pet weight is " + petWeight + " kg and the age is " + petAge);

        // create an object, declare at least 3 variables using different data types and print
    }
}
